import 'package:flutter/material.dart';
import 'package:flutter_conf_pull_to_refresh/bow.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Bow();
  }
}
